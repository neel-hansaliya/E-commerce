import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: ['st.depositphotos.com', 'img.joomcdn.net', 'm.media-amazon.com'], // Add all required domains
  },
};


export default nextConfig;